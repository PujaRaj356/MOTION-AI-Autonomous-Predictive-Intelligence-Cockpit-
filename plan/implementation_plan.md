# Project Alignment Plan: AI Predictive Maintenance Intelligence Cockpit

This plan outlines how we will adapt the current `MOTION-AI` project to perfectly align with your academic evaluation rubric, milestones, and deliverables.

## User Review Required
> [!IMPORTANT]
> Please review the proposed alignment below. Let me know which milestone you would like me to work on first (e.g., generating the Proposal, writing the User Stories, or creating the EDA notebooks).

## 1. Project Proposal & Definition
The project perfectly fits the **Intermediate/Advanced** scoping level. We will formalize it with the following details:
- **Title**: Autonomous Predictive Intelligence Cockpit for Industrial Maintenance
- **Problem Statement**: Predicting industrial machine failure and Remaining Useful Life (RUL) to minimize downtime.
- **ML Task Type**: Classification (Failure prediction) & Regression (RUL estimation).
- **Models**: XGBoost, Keras LSTM, SHAP Explainer.
- **Metrics**: F1-Score, Precision/Recall (for imbalanced failure classification), RMSE (for RUL regression).

## 2. Mandatory User Stories
We will document the mandatory User Stories in the standard Agile format as requested.
**Example:**
* **User Story (Product):** As a factory manager, I want to see a real-time health dashboard of all CNC machines, so that I can schedule maintenance before a breakdown occurs.
* **ML Task:** As a data scientist, I want to train an XGBoost classifier on historical telemetry data, so that I can predict machine failure within the next 24 hours.
* **Acceptance Criteria:** Recall ≥ 0.85 on the failure class; Inference latency < 200ms.

## 3. Milestone Tracking & Deliverables
We will follow your timeline rigorously. Since we already have a working backend and frontend, we will reverse-engineer or construct the required academic artifacts to match the milestones:

*   **[Sep 02] Proposal**: I will generate the formal 1-page proposal document.
*   **[Sep 04] Literature Review**: I will write a summary of existing predictive maintenance approaches.
*   **[Sep 09 - Sep 11] Data & Preprocessing**: We will ensure a Jupyter Notebook is present demonstrating the data cleaning pipeline.
*   **[Sep 16] EDA**: I will create an `EDA.ipynb` notebook with visualizations (correlation matrices, sensor distributions).
*   **[Sep 18 - Sep 25] Modeling**: I will create training notebooks that explicitly show the Baseline model (e.g., Logistic Regression), the Candidate models (XGBoost, LSTM), and the Hyperparameter tuning process.
*   **[Oct 02] Final Report**: I will compile the final report using the exact Chapter 1 to 7 structure provided in Part B.

## Proposed Next Steps
Once you approve this plan, I can immediately generate the **Project Proposal**, the **User Stories**, or start setting up the **Jupyter Notebooks** for your EDA milestone (which is due today, Sep 16). 
